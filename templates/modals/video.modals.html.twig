<div class="modal fade" id="modalVideo{{video.id}}" tabindex="-1" aria-labelledby="modalVideoLabel" aria-hidden="true">
    <div class="modal-dialog d-flex align-items-center">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Delete Trick</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure to want to delete this video?
            </div>
            <div class="modal-footer">
                <form class="col-6" method="post" action="{{ path('media_delete_video', {'id': video.id}) }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ video.id) }}">
                    <button class="btn btn-outline-success w-100" >Sure !</button>
                </form>
                <button type="button" class="btn btn-outline-danger col-6" data-bs-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>